/*ALGORITHME
VARIABLES
  a[50]: tableau entier
  n,i,j,max,min,pos_max,pos_min: entier
DEBUT
  AFFICHER "Entrez la taille du tableau"
  AFFICHER "n="
  LIRE (n)
  //Remplir le tableau
  AFFICHER "Remplissez le tableau"
  j=1
  POUR (i=0;i<n;i++)
      AFFICHER "Dans la case n°"
      AFFICHER (j)
      AFFICHER ",on ajoute "
      LIRE ( a[i])
      j++
  FIN POUR
  // Chercher max et min ainsi que leur position
  max=a[0]
  pos_max=1
  POUR  (i=0;i<n;i++)
      SI (max<a[i])
          max=a[i]
          pos_max=i+1
      FIN SI
  FIN POUR
  min=a[0]
  pos_min=1
  POUR  (i=0;i<n;i++)
      SI (min>a[i])
          min=a[i]
          pos_min=i+1
      FIN SI
  FIN POUR
  AFFICHER "La valeur maximale du tableau est  , qui se trouve dans la case \n",max,pos_max)
  AFFICHER "La valeur minimale du tableau est  , qui se trouve dans la case \n",min,pos_min)
FIN */
#include <stdio.h>
int main()
{
  int a[50];
  int n,i,j,max,min,pos_max,pos_min;
  printf ("Entrez la taille du tableau \nn=");
  scanf ("%d",&n);
  //Remplir le tableau
  printf ("Remplissez le tableau\n");
  j=1;
  for (i=0;i<n;i++)
    {
      printf ("Dans la case n° %d, on ajoute ",j);
      scanf ("%d",& a[i]);
       j++;
    }
  // Chercher max et min ainsi que leur position
  max=a[0];
  pos_max=1;
  for  (i=0;i<n;i++)
    {
      if (max<a[i])
        {
          max=a[i];
          pos_max=i+1;
        }
      }
  min=a[0];
  pos_min=1;
  for  (i=0;i<n;i++)
    {
      if (min>a[i])
        {
          min=a[i];
          pos_min=i+1;
        }
      }
  printf ("La valeur maximale du tableau est %d , qui se trouve dans la case %d\n",max,pos_max);
  printf ("La valeur minimale du tableau est %d , qui se trouve dans la case %d\n",min,pos_min);
  return(0);
}
